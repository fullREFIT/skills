# Setup Complete — Final Report

**Repo:** `<owner/repo>`
**Installed at:** `<absolute path>`
**Setup duration:** `<approximate minutes>`
**Date:** `<ISO date>`

---

## Stack

- `<language + version>` (pinned via `<.nvmrc / .python-version / etc.>`)
- `<additional languages>`
- `<containerization if used>`

## Toolchain State

**Already installed (verified, not modified):**
- `<tool version>`
- `<tool version>`

**Installed fresh during setup:**
- `<tool version — install source>`

**Version managers used:**
- `<nvm / pyenv / rustup / asdf / mise>`

## Dependencies Installed

- `<package manager>` dependencies via `<command>`
- `<count>` packages (approximate)
- Lockfile: `<filename>` — `<up-to-date / regenerated / n/a>`

## Environment Configuration

**`.env` file:** `<created from template / already existed / not required>`

**Required env vars:**
- [ ] `<VAR_NAME>` — `<filled / still blank>`
- [ ] `<VAR_NAME>` — `<filled / still blank>`

If any vars are still blank, the app will not start correctly. Fill them before running.

## Smoke Test

**Test run:** `<command or "SKIPPED">`

**Result:** `<PASSED / FAILED / SKIPPED>`

**Reason (if skipped):** `<e.g., "Tests require live Postgres">`

**Output summary (if run):**
```
<first 10 lines of test output>
```

## Next Steps

1. `<First manual action — e.g., "Fill DATABASE_URL in .env">`
2. `<Second manual action — e.g., "Run `docker compose up -d postgres`">`
3. `<Verification step — e.g., "Visit http://localhost:3000 and confirm home page renders">`

## Issues Encountered

Populate honestly. If none, write `none`. Empty section without "none" = invalid report.

- `<issue 1 — e.g., "Lockfile mismatch between package.json and package-lock.json; fell back to npm install with user permission">`
- `<issue 2 — e.g., "Skipped smoke test because test suite requires Stripe test mode secrets">`

## Deviations from Standard Flow

If the user overrode any rule (skipped phases, used global install, bypassed ambiguity gate), note it here so there's an audit trail.

- `<deviation, or "none">`

---

*Generated by github-repo-setup skill. Setup is complete. Decide next action manually.*
