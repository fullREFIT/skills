# Scripts

This skill is instruction-based (Claude follows the protocol in SKILL.md), not script-based. No executable scripts are required.

If you want to extend the skill with helpers — for example, a script that measures actual token usage per Codex CLI invocation and updates a running cost log — add them here.
