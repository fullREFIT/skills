{
  "name": "fullrefit-presentation-deck-builder",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite site --host 127.0.0.1",
    "build": "vite build site --outDir ../dist --emptyOutDir",
    "test": "vitest run",
    "audit:public": "node scripts/audit-public.mjs",
    "check": "npm run audit:public && npm test && npm run build"
  },
  "devDependencies": {
    "axe-core": "^4.12.1",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
